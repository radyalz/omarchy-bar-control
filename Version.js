.pragma library

// Single source of truth for the version string shown in both surfaces
// (the settings app's About card and the bar popover header). Update `number`
// here and both panels follow. Keep it in step with the version field in
// manifest.json.
var name = "Radyalz Bar Control"
var number = "0.4.4"
var full = "v" + number
var author = "Radman Alizadeh (Radyalz)"
var license = "MIT"
